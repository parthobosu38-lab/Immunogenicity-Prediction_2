# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
requests>=2.28

# Machine learning
scikit-learn>=1.3
xgboost>=1.7

# Bioinformatics (UniProt sequence handling, ProteinAnalysis, BLOSUM62)
biopython>=1.81

# Model explainability (notebook 04)
shap>=0.44
lime>=0.2.0.1

# Fuzzy sequence matching for near-duplicate removal (notebook 05)
rapidfuzz>=3.0

# Plotting
matplotlib>=3.7
seaborn>=0.12

# --- Optional: ESM-2 protein language model embeddings (notebooks 04 & 06) ---
# Only needed if Config.use_esm_embeddings = True. CPU wheels shown below;
# for GPU, install a CUDA build of torch instead before running this file.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0
fair-esm>=2.0.0
